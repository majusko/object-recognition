# This project is part of a bachelor thesis.
# It is used here OpenCV library.
# Android application implemented algorithm uses three OpenCV library.
# The plan is to use them so that I could recognize traffic signs.